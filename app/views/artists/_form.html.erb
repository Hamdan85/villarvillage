<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="large-3 panel columns">
      <%= image_tag 'missing_avatar.gif' %>
      <%= f.file_field :avatar %>

      <h4><%= f.text_field :name, :placeholder => 'Nome' %></h4>

      <h4><%= f.text_field :country, :placeholder => 'País' %></h4>
    </div>

    <div class="large-9 columns">

      <div class="row">
        <div class="large-12 columns">
          <div class="panel">
            <h3>Release</h3>
            <p><p><%= f.text_area :release %></p><hr></p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="large-12">
      <%= f.submit 'Registrar', :class => 'small button expand' %>
      <%= link_to 'Voltar aos Artistas', artists_path, :class => 'small button expand' %>
    </div>
  </div>
<% end %>

